const config = {
  semi: true,
  singleQuote: true,
  printWidth: 200, // already large, but you can increase it further if needed
  tabWidth: 2,
  useTabs: false,
  trailingComma: 'none', // can help reduce line length
  bracketSpacing: true,
  proseWrap: 'never', // for markdown-style wrapping
  htmlWhitespaceSensitivity: 'ignore'
};

module.exports = config;
